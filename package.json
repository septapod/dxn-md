{
  "name": "dxn-md",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "dxn.md — the agent-native companion site to dxn.is. Every surface generated from a canonical facts layer.",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "tsx scripts/build.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "refresh": "node scripts/refresh-rss.mjs",
    "changes": "tsx scripts/regen-changes.ts",
    "ping": "node scripts/ping-indexnow.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "@vercel/functions": "^3.1.0",
    "fast-xml-parser": "^5.8.0",
    "gpt-tokenizer": "^3.0.0",
    "mcp-handler": "^1.1.0",
    "yaml": "^2.5.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.8"
  }
}
